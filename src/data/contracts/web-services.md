# Page contract: web-services

Source: https://www.pugetsoundinfo.wa.gov/WebServices/Index (fetched 2026-07-17) / Route(s): /prototypes/web-services

Header: plain — "Web Services" (source-sourced lede: "Web Services are functions that can be accessed over the web (using the http protocol), typically consumed by software or programs rather than humans. The services we provide here allow programmatic access to PS Info data. The intent of these web services is to facilitate coordination and information sharing between agencies, interested parties, and the public within and outside of the Puget Sound.")

Use `import EsaPageHeader from '@esa/ecology/esa-page-header.astro';` with `title="Web Services"` and the lede above. Do NOT use PsInfoPhotoHeader / a photo band — the source page carries no imagery beyond site chrome, and this unit's header treatment is `plain`.

## Sections (source order, exhaustive)

The source is a single flat section: an intro paragraph followed by 28 web-service entries in a fixed order (alphabetical by title, not an authored grouping — no category sub-headings exist on the page). Each entry has: a bold ~20px title, a one-line description, an "Example Urls:" label with two links (CSV, JSON), a "Parameters:" line, and — for 18 of the 28 — a "Last Updated Date:" line and a "Change Notes:" line (which may stack multiple dated notes via `<br/>`). A `<hr>` visually separates every entry, including after the last one (chrome, not content).

1. **Intro copy** — verbatim: "Web Services are functions that can be accessed over the web (using the http protocol), typically consumed by software or programs rather than humans. The services we provide here allow programmatic access to PS Info data. The intent of these web services is to facilitate coordination and information sharing between agencies, interested parties, and the public within and outside of the Puget Sound."

2. **Available services** (28 entries, in this exact order):

   1. **2022-2026 Action Agenda Desired Outcome By ID** — "Returns the Desired Outcome for a given Outcome ID" — URLs: `/WebServices/GetOutcomeByID/CSV/1`, `/WebServices/GetOutcomeByID/JSON/1` — Parameters: Return Type, OutcomeID — Last Updated: 05/19/2022 — Change Notes: "05/19/2022: Added as new web service."
   2. **2022-2026 Action Agenda Desired Outcomes** — "Returns a list of all Desired Outcomes" — URLs: `/WebServices/GetOutcomes/CSV`, `/WebServices/GetOutcomes/JSON` — Parameters: Return Type — Last Updated: 05/19/2022 — Change Notes: "05/19/2022: Added as new web service."
   3. **2022-2026 Action Agenda Strategies** — "Returns a list of all Strategies" — URLs: `/WebServices/GetStrategies/CSV`, `/WebServices/GetStrategies/JSON` — Parameters: Return Type — Last Updated: 05/19/2022 — Change Notes: "05/19/2022: Added as new web service."
   4. **2022-2026 Action Agenda Strategy By ID** — "Returns the Strategy for a given Strategy ID" — URLs: `/WebServices/GetStrategyByID/CSV/1`, `/WebServices/GetStrategyByID/JSON/1` — Parameters: Return Type, StrategyID — Last Updated: 05/19/2022 — Change Notes: "05/19/2022: Added as new web service."
   5. **Fund by ID** — "Returns the Fund for a given FundID" — URLs: `/WebServices/GetFundByID/CSV/1`, `/WebServices/GetFundByID/JSON/1` — Parameters: Return Type, FundID — (no Last Updated / Change Notes on source)
   6. **Funds** — "Returns a list of all Funds" — URLs: `/WebServices/GetFunds/CSV`, `/WebServices/GetFunds/JSON` — Parameters: Return Type — (no Last Updated / Change Notes)
   7. **NEP Activities** — "Returns a list of all NEP Activities" — URLs: `/WebServices/GetNEPActivities/CSV`, `/WebServices/GetNEPActivities/JSON` — Parameters: Return Type — Last Updated: 04/05/2023 — Change Notes: "12/01/2022: Added Related Activities column" / "04/05/2023: Added Action Agenda Strategies column"
   8. **NEP Activity by ID** — "Returns the NEP Activity for a given NEP Activity" (verbatim source wording — self-referential, not "…NEP Activity ID"; preserve as-is) — URLs: `/WebServices/GetNEPActivityByID/CSV/13`, `/WebServices/GetNEPActivityByID/JSON/13` — Parameters: Return Type, NEP ActivityID — Last Updated: 04/05/2023 — Change Notes: "12/01/2022: Added Related Activities column" / "04/05/2023: Added Action Agenda Strategies column"
   9. **NEP Activity Expenditures** — "Returns a list of all NEP Activity Expenditure records" — URLs: `/WebServices/GetNEPActivityExpenditure/CSV`, `/WebServices/GetNEPActivityExpenditure/JSON` — Parameters: Return Type — Last Updated: 12/01/2022 — Change Notes: "12/01/2022: Added web service"
   10. **NEP Activity Expenditures by Activity ID** — "Returns a list of all NEP Expenditure records for a given NEPActivityID" — URLs: `/WebServices/GetNEPActivityExpenditureByNEPActivityID/CSV/13`, `/WebServices/GetNEPActivityExpenditureByNEPActivityID/JSON/13` — Parameters: Return Type, NEP ActivityID — Last Updated: 12/01/2022 — Change Notes: "12/01/2022: Added web service"
   11. **NEP Activity Reported Funding** — "Returns a list of all NEP Activity Funding records" — URLs: `/WebServices/GetNEPActivityReportedFunding/CSV`, `/WebServices/GetNEPActivityReportedFunding/JSON` — Parameters: Return Type — Last Updated: 12/01/2022 — Change Notes: "12/01/2022: Renamed \"Financials\" to \"Reported Funding\""
   12. **NEP Activity Reported Funding by Activity ID** — "Returns a list of all NEP Funding records for a given NEPActivityID" — URLs: `/WebServices/GetNEPActivityReportedFundingByNEPActivityID/CSV/13`, `/WebServices/GetNEPActivityReportedFundingByNEPActivityID/JSON/13` — Parameters: Return Type, NEP ActivityID — Last Updated: 12/01/2022 — Change Notes: "12/01/2022: Renamed \"Financials\" to \"Reported Funding\""
   13. **NEP Award by ID** — "Returns the NEP Award for a given NEPAwardID" — URLs: `/WebServices/GetNEPAwardByID/CSV/1`, `/WebServices/GetNEPAwardByID/JSON/1` — Parameters: Return Type, AwardID — (no Last Updated / Change Notes)
   14. **NEP Awards** — "Returns a list of all NEP Awards" — URLs: `/WebServices/GetNEPAwards/CSV`, `/WebServices/GetNEPAwards/JSON` — Parameters: Return Type — (no Last Updated / Change Notes)
   15. **Ongoing Program by ID** — "Returns the OngoingProgram for a given OngoingProgramID" — URLs: `/WebServices/GetOngoingProgramByID/CSV/6`, `/WebServices/GetOngoingProgramByID/JSON/6` — Parameters: Return Type, Ongoing ProgramID — (no Last Updated / Change Notes)
   16. **Ongoing Program Financials** — "Returns a list of all Ongoing Program Financial records for a given OngoingProgramID" — URLs: `/WebServices/GetOngoingProgramFinancials/CSV`, `/WebServices/GetOngoingProgramFinancials/JSON` — Parameters: Return Type — (no Last Updated / Change Notes)
   17. **Ongoing Program Financials by Ongoing Program ID** — "Returns a list of all Ongoing Program Financial records for a given OngoingProgramID" — URLs: `/WebServices/GetOngoingProgramFinancialsByOngoingProgramID/CSV/6`, `/WebServices/GetOngoingProgramFinancialsByOngoingProgramID/JSON/6` — Parameters: Return Type, Ongoing ProgramID — (no Last Updated / Change Notes)
   18. **Ongoing Programs** — "Returns a list of all Ongoing Programs" — URLs: `/WebServices/GetOngoingPrograms/CSV`, `/WebServices/GetOngoingPrograms/JSON` — Parameters: Return Type — (no Last Updated / Change Notes)
   19. **Progress Indicator by ID** — "Returns a list of all Progress Indicators records for a given ProgressIndicatorID" — URLs: `/WebServices/GetProgressIndicatorByID/CSV/19`, `/WebServices/GetProgressIndicatorByID/JSON/19` — Parameters: Return Type, Progress IndicatorID — Last Updated: 05/19/2022 — Change Notes: "05/19/2022: Update terminology: \"Progress Measure\" is now \"Puget Sound Indicator\"; \"Intermediate Progress Measure\" is now \"Progess Indicator\"." (verbatim, including source's own misspelling "Progess Indicator")
   20. **Progress Indicators** — "Returns a list of all Progress Indicators" — URLs: `/WebServices/GetProgressIndicators/CSV`, `/WebServices/GetProgressIndicators/JSON` — Parameters: Return Type — Last Updated: 05/19/2022 — Change Notes: same terminology note as #19 (verbatim)
   21. **Puget Sound Indicator by ID** — "Returns the published Puget Sound Indicator for a given PugetSoundIndicatorID" — URLs: `/WebServices/GetPugetSoundIndicatorByID/CSV/1`, `/WebServices/GetPugetSoundIndicatorByID/JSON/1` — Parameters: Return Type, Puget Sound IndicatorID — Last Updated: 05/19/2022 — Change Notes: same terminology note (verbatim)
   22. **Puget Sound Indicator Reported Values** — "Returns a list of all Puget Sound Indicator Reported Values for published Puget Sound Indicators" — URLs: `/WebServices/GetPugetSoundIndicatorValues/CSV`, `/WebServices/GetPugetSoundIndicatorValues/JSON` — Parameters: Return Type — Last Updated: 05/19/2022 — Change Notes: same terminology note (verbatim)
   23. **Puget Sound Indicator Reported Values by Puget Sound Indicator ID** — "Returns a list of all Puget Sound Indicator Reported Values for a given published PugetSoundIndicatorID" — URLs: `/WebServices/GetPugetSoundIndicatorValuesByPugetSoundIndicatorID/CSV/1`, `/WebServices/GetPugetSoundIndicatorValuesByPugetSoundIndicatorID/JSON/1` — Parameters: Return Type, Puget Sound IndicatorID — Last Updated: 05/19/2022 — Change Notes: same terminology note (verbatim)
   24. **Puget Sound Indicators** — "Returns a list of all published Puget Sound Indicators" — URLs: `/WebServices/GetPugetSoundIndicators/CSV`, `/WebServices/GetPugetSoundIndicators/JSON` — Parameters: Return Type — Last Updated: 05/19/2022 — Change Notes: same terminology note (verbatim)
   25. **Vital Sign by ID** — "Returns the Vital Sign for a given VitalSignID" — URLs: `/WebServices/GetVitalSignByID/CSV/2`, `/WebServices/GetVitalSignByID/JSON/2` — Parameters: Return Type, Vital SignID — (no Last Updated / Change Notes)
   26. **Vital Sign Indicators** — "Returns a list of all Vital Sign Indicators" — URLs: `/WebServices/GetVitalSignIndicators/CSV`, `/WebServices/GetVitalSignIndicators/JSON` — Parameters: Return Type — Last Updated: 05/19/2022 — Change Notes: same terminology note (verbatim)
   27. **Vital Sign Indicators by Vital Sign ID** — "Returns a list of all Vital Sign Indicators records for a given VitalSignID" — URLs: `/WebServices/GetVitalSignIndicatorByVitalSignID/CSV/2`, `/WebServices/GetVitalSignIndicatorByVitalSignID/JSON/2` — Parameters: Return Type, Vital SignID — Last Updated: 05/19/2022 — Change Notes: same terminology note (verbatim)
   28. **Vital Signs** — "Returns a list of all Vital Signs" — URLs: `/WebServices/GetVitalSigns/CSV`, `/WebServices/GetVitalSigns/JSON` — Parameters: Return Type — (no Last Updated / Change Notes)

   All URLs are relative to `https://www.pugetsoundinfo.wa.gov` (e.g. `/WebServices/GetOutcomeByID/CSV/1` → `https://www.pugetsoundinfo.wa.gov/WebServices/GetOutcomeByID/CSV/1`). Each is a real, live download endpoint on the source domain. Per this prototype's link convention, `*.pugetsoundinfo.wa.gov` destinations are INTERNAL — rendered same-tab with no external-link mark (`isExternalHref` classifies them internal); the visible link text is prod's verbatim relative path, the href the absolute endpoint URL.

## Existing prototype/data file assessment

`src/data/pages/web-services.ts` is **already a complete, verbatim, byte-for-byte-accurate extraction** of all 28 services (titles, descriptions, both example URLs, parameters, last-updated dates, and change notes) — every entry cross-checked line-by-line against the fetched source and matches exactly, including the source's own oddities (the self-referential "NEP Activity by ID" description, and the "Progess Indicator" misspelling in the terminology change notes). Nothing in the data file is invented or extra. Keep it as-is.

`src/pages/prototypes/web-services.astro` is content-correct (intro paragraph + the 28-item catalog) but its **header treatment must change**: it currently renders `PsInfoPhotoHeader` with `size="band"` and `/photos/sound-aerial.jpg`. This unit's assigned header treatment is **plain**. Replace with `EsaPageHeader` (title="Web Services", lede = the intro paragraph verbatim) and remove the photo-header import/usage. The "Available services" section title with a `${TOTAL_SERVICES} services` count badge is a derived count of the real, verbatim 28-item list (not a fabricated stat) and may be kept.

## Explicit exclusions

- Nothing beyond the sections above. No invented ledes/taglines/eyebrows, no stat bands the source doesn't show, no cross-link bands, no photos the source doesn't carry.
- No category/group headings ("Action Agenda", "Funds", "NEP", "Ongoing Programs", "Progress Indicators", "Puget Sound Indicators", "Vital Signs") — the source's apparent grouping is purely alphabetical sort order, not an authored IA element. Do not add sub-headings that don't exist on the source.
- No photo header / photo band for this page — source carries no imagery beyond site chrome; header is `plain` (EsaPageHeader).
- No additional metadata fields beyond what each entry actually carries (title, description, 2 example URLs, parameters, and — only where present on source — last-updated date and change notes). Exactly 10 of the 28 entries have neither a "Last Updated Date" nor "Change Notes" line on the source — Fund by ID, Funds, NEP Award by ID, NEP Awards, Ongoing Program by ID, Ongoing Program Financials, Ongoing Program Financials by Ongoing Program ID, Ongoing Programs, Vital Sign by ID, Vital Signs — do not add those rows to those 10 entries. The remaining 18 entries have both.

## Gaps

None. The entire page is static server-rendered HTML (no JS-rendered content observed) and every section, label, link, and note was extracted directly from the fetched source with no missing or inaccessible content.

## Fidelity audit — 2026-07-22

Structure + content re-audit against the saved prod DOM (scratchpad/prod/webservices.html):

- **Data verified byte-for-byte**: all 28 entries programmatically diffed (title, description, both example URLs, parameters, last-updated, change notes) — zero mismatches. Entry data untouched.
- **Removed (invented, not on prod)**: the "Available services" h2 + "28 services" count badge (prod runs entries directly after the intro with no heading), and the CSV/JSON label chips on URL rows (prod's anchors show only the relative path). `TOTAL_SERVICES` export deleted with the badge.
- **Restored (prod labels, verbatim)**: "Example Urls:" line above each entry's URL rows (×28); "Last Updated Date: {date}" (was rephrased "Last updated {date}"); "Change Notes:" label ahead of the note lines (×18).
- **Aligned**: visible endpoint link text is now prod's site-relative path (href stays absolute). Endpoint links remain same-tab/no-mark — `*.pugetsoundinfo.wa.gov` is internal per the repo-wide link convention (supersedes this contract's earlier external-mark note).
- **Corrected count**: 18 of 28 entries (not 17) carry both "Last Updated Date:" and "Change Notes:" on prod — the 10 without are listed under Explicit exclusions.
- **Kept as established chrome/composition** (not prod-visual): EsaPageHeader h1 with the intro paragraph as lede (prod: breadcrumb title + intro as first body text — same reading order); one collapsed-by-default esa-collapsible per entry (prod: flat expanded list with `<hr>` separators — content parity preserved, native `<details>` keyboard semantics). Heading tree is a single h1, matching prod's heading-less main.
